#include <stdio.h>

#define OCIOSO "aguardando uma chamada\n"
#define ESTACIONADO "parado\n"
#define SUBINDO "subindo\n"
#define DESCENDO "descendo\n"

int main() {

    // 2. Escreva um código que defina um conjunto de constantes para representar os estados de um elevador com respeito ao seu movimento: 
    // ocioso (aguardando uma chamada), estacionado (parado em certo andar para alguém entrar ou sair), subindo ou descendo.

    printf(OCIOSO);
    printf(ESTACIONADO);
    printf(SUBINDO);
    printf(DESCENDO);

    return 0;
  
}